/**
 * Package that contains the classes needed to construct a LobbyView for the application.
 * GameStarters are used to launch games by passing files into the Validation module, combining an Icon and its description into a clickable VBox.
 * FlowPane is used to translate all XML data into a user-friendly selection of Icons.
 * VBox wraps this FlowPane with a SettingsBar for the full display.
 * @author Eric Doppelt
 */
package UI.LobbyView;